# Container support files

- `containers/bin/` contains wrapper scripts added to the image.
- Drop manually-downloaded third-party tool archives under `third_party/` in the
  repo root so the Dockerfile can vend them.
